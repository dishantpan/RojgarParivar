# RojgarParivar — Frontend

React.js based UI for workers and employers.

## Setup

1. `npm install`
2. Create `.env`:
REACT_APP_API_URL=http://localhost:5000
3. `npm start`

## Features
- Hindi language interface
- Worker profile creation
- Job browsing and applying
- Employer job posting